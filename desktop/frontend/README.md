# Frontend Renderer (React + Vite + Zustand)

This folder is the independent renderer used by the Electron desktop app.

## Commands

Install dependencies:

```bash
npm install
```

Run in development:

```bash
npm run dev
```

Build renderer:

```bash
npm run build
```

Preview build:

```bash
npm run preview
```
